# Ores N' Stuff

A very WIP mod that adds more ores, ingots, and etc.

## Building Instructions

To build the project, first run ``./gradlew genSources`` (or ``gradlew.bat genSources`` on Windows), then run ``./gradlew build`` (or ``gradlew.bat build`` on Windows).
